;; Secrets for various systems.

;; use encrypted storage for passwords. :)
;;
;; (defun load-secrets ()
;;   (interactive)
;;   (load-library "secrets.el.gpg"))

;;(load-library "secrets.el.gpg")
(provide 'secrets)
