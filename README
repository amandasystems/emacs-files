This is my emacs configuration files. The .emacs is (as usual) in my $HOME, and most other files are in ~/.emacs.d/. I keep passwords and such in ~/.emacs.d/secrets.el, which for obvious reasons isn't distributed. I will split my .emacs into more files in the future.v